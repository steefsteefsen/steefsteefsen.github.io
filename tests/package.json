{
  "name": "steefsteefsen-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "test:ci": "jest --ci"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "coverageDirectory": "../logs/coverage",
    "coverageReporters": ["text", "cobertura"]
  }
}
